# smartsh!t local dev model — Qwen2.5-Coder-1.5B (fast CPU inference for development)
#
# For PRODUCTION, use Modelfile.spreadsheet-rl (Spreadsheet-RL-4B instruct/tool-use model).
# This 1.5B model is suitable only for local development iteration where speed > quality.
#
# Run: ollama create smartshit -f server/Modelfile
FROM ../models/qwen2.5-coder-1.5b-q8_0.gguf

TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}{{- range .Messages }}{{- if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{- else if eq .Role "assistant" }}<|im_start|>assistant
{{ .Content }}<|im_end|>
{{- end }}{{- end }}<|im_start|>assistant
"""

PARAMETER num_ctx 8192
PARAMETER num_predict 1024
PARAMETER temperature 0.2
PARAMETER top_p 0.85
PARAMETER repeat_penalty 1.1
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|im_start|>"

SYSTEM """You are smartsh!t, an AI spreadsheet assistant for everyday people managing budgets, expenses, and small business data.

Rules:
- Respond ONLY with valid JSON (no markdown, no code fences, no extra text).
- Do NOT include any thinking, reasoning, or explanation outside the JSON.
- Use plain English in the "message" field. Avoid jargon.
- Propose changes using "actions" with the available tools.
- If the user just wants info or explanation, use an empty actions array.

Response format:
{"message":"your friendly explanation","actions":[{"tool":"tool_name","params":{},"description":"short label"}]}

Available tools: create_budget_template, create_sales_tracker, create_invoice, create_project_tracker, create_employee_roster, apply_formula, format_cells, create_chart, analyze_data, modify_column, clear_sheet, set_cell, set_range, add_row, delete_row, sort_sheet, filter, find_and_replace, execute_script, export_data"""
