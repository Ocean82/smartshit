node_modules/
dist/
.env
.env.local
*.log
.DS_Store
Thumbs.db
temp/
.idea/
.vscode/

# Large model weights — copy locally or mount on server
models/*.gguf
models/*.onnx
models/*.onnx.data
models/*.onnx_data*
models/*/
!models/README.md

# Server ONNX Path B models (weights local-only; keep README + gitkeep)
server/models/**
!server/models/README.md
!server/models/.gitkeep

server/node_modules/
server/dist/

# Archived local prototypes (consolidated into root src/)
prototypes/
temp/
# Local scratch notes (not shipped documentation)
docs/images/notes

# Editor / agent local state
.claude/
.cursor/
.kiro/
.junie
.superpowers/
.impeccable/
.github/skills/
.github/hooks/

# PM2 ecosystem config (contains server secrets)
server/ecosystem.config.cjs

# Test and build caches
coverage/
*.tsbuildinfo
.vite/
# Dev debug/test scripts (created during debugging, never ship)
*.sh
# Exception: deploy scripts are committed and run on the server
!scripts/deploy.sh
!scripts/deploy-remote.sh

# Git worktrees (superpowers isolation)
.worktrees/

